This assignment was turned in on 9/11.

Purpose (taken directly from the assignemnt page): Each program should allow the user to control the encryption key, which will be provided via a file with an arbitrary file name. The program will read plain text from a file, encrypt it using the selected algorithm, and write the cipher text to an output file.  
